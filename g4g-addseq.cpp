//additive sequence
#include<stdio.h>
#include<cstring>
using namespace std;
int main() {
	char str[201];
	int T=0;
	scanf("%d", &T);
	for (int i = 0; i < T; i++) {
		scanf("%s", str);
		for (int i = 0; i < strlen(str); i++) {

		}
	}
}